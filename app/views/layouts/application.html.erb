<!DOCTYPE html>
<html>
  <head>
    <title>Flats</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <nav>
      <% if property_owner_signed_in? %> 
        <ul>
          <li><%= current_property_owner.email %></li> 
          <li><%= link_to 'Sair', destroy_property_owner_session_path, method: :delete %></li>
          <li><%= link_to('Cadastre seu imóvel aqui', new_property_path)%></li>
          <li><%= link_to('Meus imóveis', my_properties_properties_path)%></li>
        </ul>
        <div>=================================================================</div>
      <% else %>
        <p><%= link_to 'Entrar como proprietário', new_property_owner_session_path %></p>
      <% end %>
      <%= %>
    </nav>
    <p> <%= notice %> </p>
    <p> <%= alert %> </p>
    <%= yield %>
  </body>
</html>