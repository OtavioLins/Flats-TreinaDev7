<!DOCTYPE html>
<html>
  <head>
    <title>Flats</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <nav>
      <% if property_owner_signed_in? %> 
          <%= current_property_owner.email %></li> ||
          <%= link_to('Cadastre seu imóvel aqui', new_property_path)%> ||
          <%= link_to('Meus imóveis', my_properties_properties_path)%> ||
          <%= link_to 'Sair', destroy_property_owner_session_path, method: :delete %> 
        <div>=================================================================</div>
      <% elsif user_signed_in? %>
          <%= current_user.email %> ||
          <%= link_to 'Sair', destroy_user_session_path, method: :delete %> 
      <% else %>
        <p><%= link_to 'Entrar como proprietário', new_property_owner_session_path %> || 
        <%= link_to 'Entrar como usuário', new_user_session_path %></p>
      <% end %>
      <%= %>
    </nav>
    <p> <%= notice %> </p>
    <p> <%= alert %> </p>
    <%= yield %>
  </body>
</html>